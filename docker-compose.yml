version: "3.8"

services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: appdb

  api:
    build: ./api-node
    ports:
      - "3000:3000"
    depends_on:
      - db